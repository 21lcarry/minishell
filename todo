1) Не обрабатываем ; \, незакрытые ковычки, пайп в конце
2) 1 глобальная переменная
3) msh$:
4) Реализовать историю
5) Выполнение команд, абсолютный и относительный(env PATH)
6) Встроенные команды:
+   2 1. echo -n +
-   6 2. cd
+   3 3. pwd +
+   4 4. export
-   7 5. unset
+   5 6. env +
    8. exit
7) Реализовать ''
8) Реализовать ""
9) Пайпы
10) Переменные окружения $VAR
11) Реализовать $?
12) Сигналы ctrl-C ctrl-D ctrl-\

###malloc##
struct main;
str** main->env (указатель на массив, ячейки массива);
struct **cmd main->cmd (указатель на массив структур, ячейки массива)
char **main->cmd->command

!ошибки во второй канал
!!!return до free(проверить другие обработки ошибок) в функциях обрабатывающих fd!!!
!!!ft_cd free(sigfault) getenv()
exit builtin

возможный лик - ft_add_env2
main->env[i] = ft_strdup(tmp[i]);

######
задефайнить потоки
структура для сигналов
вызов signal();